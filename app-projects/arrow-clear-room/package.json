{
  "name": "arrow-clear-room",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5330 --strictPort frontend",
    "build": "vite build frontend --outDir ../dist --emptyOutDir",
    "verify": "node scripts/verify-levels.js && node scripts/verify-local.js",
    "verify:levels": "node scripts/verify-levels.js"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "playwright-core": "^1.61.1"
  }
}
